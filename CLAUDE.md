# 프로젝트 규칙

nyanfly — 냥이를 대포로 날려 거리를 겨루는 원버튼 물리 아케이드.
설계 근거는 `docs/superpowers/specs/2026-09-22-nyanfly-design.md`에 있다.

## 최우선 규칙

**모든 작업은 최적화와 보안 이슈가 생기지 않게 한다.** 기능을 추가·수정할 때마다 아래를
확인하고, 문제가 될 만한 점은 작업 안에서 같이 해결한다.

## 결정론 규칙 (가장 중요)

`js/sim/` 안의 코드는 같은 입력에 항상 같은 결과를 내야 한다. 이게 깨지면 리플레이,
서버 기록 검증, 데일리 코스 공정성이 전부 무의미해진다. 다음을 절대 쓰지 않는다.

1. `Math.random()` — `js/sim/rng.js`의 해시·난수기만 쓴다.
2. `Date.now()`, `performance.now()` — 시간은 프레임 번호로만 표현한다.
3. DOM, Canvas 접근 — 시뮬은 화면을 모른다.
4. `Math.sin/cos/tan/pow/exp` — IEEE 754가 정확도를 규정하지 않아 플랫폼마다 결과가
   달라질 수 있다. `js/sim/trig.js`의 다항식 근사를 쓴다.
   (`Math.sqrt/floor/round/abs/min/max/imul`은 규정되어 있어 허용)

시뮬 로직을 바꾸면 `js/sim/sim.js`의 `SIM_VERSION`을 올린다. 과거 리플레이와 랭킹은
버전이 다르면 섞이지 않는다.

입자·화면 흔들림·카메라는 `js/render/`에만 둔다. 여기서는 `Math.random()`을 써도 된다.

## 연출 규칙

손맛은 전부 표현 계층에 있다. 시뮬은 연출을 모른다.

- **히트스톱**은 `js/render/render.js`가 `view.hitstop`에 요청하고 `js/main.js`가
  그 동안 `step()` 호출을 멈추는 방식이다. 시뮬의 프레임 번호는 그대로이므로
  리플레이와 기록 검증에 영향이 없다. 연출을 시뮬 안에 넣으면 이 성질이 깨진다.
- 카메라는 추적용 `view.scale`과 실제 그리기용 `view.zoom`으로 나뉘어 있다.
  펀치 같은 순간 연출은 `zoom`에만 얹는다.
- 효과음은 `js/audio/sfx.js`에서 WebAudio로 즉석 합성한다. 음원 파일을 추가하지
  않으므로 CSP와 저장소 용량을 건드릴 일이 없다. 브라우저가 사용자 조작 전에는
  소리를 막으므로 첫 입력에서 `unlock()`을 부른다.
- 새 접촉 종류를 추가하면 `consumeFx()` 한 곳에서 입자와 소리를 같이 붙인다.
- 조준 궤적 미리보기는 `js/sim/sim.js`의 `previewPath()`를 쓴다. 렌더가 물리 상수를
  베껴 쓰면 물리를 고칠 때 미리보기가 조용히 틀어진다. 이 함수는 시뮬 상태를
  전혀 바꾸지 않는다.
- 스프라이트는 `js/render/sprites.js`에서 캔버스로 직접 그린다. 이미지 파일을 쓰지
  않으므로 저장소 용량과 로딩이 그대로다. 캐릭터는 굵은 외곽선을 먼저 깔아
  배경에서 떠오르게 한다(`outlined()`).

## 최적화 체크리스트

- 매 프레임 도는 코드(`step`, `updateView`, `render`)에서 새 배열·객체·클로저를 만들지
  않는다. 입자와 효과는 미리 만든 고정 크기 풀을 돌려 쓴다.
- 반복해서 그리는 그림은 `js/render/sprites.js`의 `sprite()`로 오프스크린 캔버스에
  캐시하고 `drawImage`로 재사용한다.
- 화면 밖은 계산도 그리기도 하지 않는다. 지형은 보이는 구간만, 오브젝트는 보이는
  슬롯만 훑는다.
- 지형과 오브젝트는 배열로 들고 있지 않고 `hash32(seed, index)`로 즉석 계산한다.
  코스 길이를 늘려도 메모리가 늘지 않는다.
- 캔버스 픽셀 수(DPR)에 상한을 둔다(`DPR_CAP`).
- 변경 후에는 **창을 띄운 실제 브라우저에서** 후반부 구간의 `render` 소요 시간을 재서
  60fps 예산(16.7ms) 안에 드는지 확인한다. 창이 숨겨져 있으면 캔버스 작업이 실제로
  수행되지 않아 측정값이 의미 없다.

## 보안 체크리스트

- 비밀 키는 저장소에 넣지 않는다. Supabase는 브라우저 공개용 publishable 키만
  `js/net/config.js`에 두고, secret/service_role 키는 절대 쓰지 않는다.
- 사용자 입력(닉네임 등)은 `textContent`로만 출력한다. `innerHTML`을 쓰지 않는다.
- `eval`, `new Function`, 신뢰할 수 없는 외부 스크립트를 쓰지 않는다.
- DB는 행 단위 보안(RLS)으로 조회·등록만 허용하고 수정·삭제는 막는다. 값의 범위
  검사와 연속 등록 제한은 DB 쪽에도 건다(`supabase/schema.sql`).
- 클라이언트가 보내는 거리는 신뢰하지 않는다. 기록에 담긴 입력으로 `replay()`를 돌려
  검증한다.
- `index.html`의 CSP 메타 태그를 유지한다. 외부 접속처를 늘릴 때만 목록에 추가한다.

## 작업 방식

- 로컬 확인: `node tools/serve.mjs` 로 서버를 띄우고 `http://localhost:5173/` 를 연다.
  ES 모듈이라 `file://` 로는 열리지 않는다.
- 시뮬을 고치면 `node --test tests/sim.test.mjs` 를 돌린다. 결정론과 리플레이 일치는
  여기서 지킨다.
- 밸런스를 고치면 `node tools/balance.mjs` 로 각도·레벨별 기록 분포를 확인한다.
  목표: 무업 300m 안팎, 풀업 1500m 이상, 업그레이드와 타이밍 실력이 각각 절반쯤 기여.
- CSS/JS를 바꿔 배포할 때는 `index.html`의 `?v=` 값을 올려 캐시를 무효화한다.
- 한국어로 커밋 메시지와 주석을 쓴다.
